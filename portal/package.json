{
  "name": "tratto-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env-file=.dev.vars",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "schema": "wrangler d1 execute tratto-portal --remote --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260907.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.129.0"
  }
}
